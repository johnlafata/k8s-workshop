apiVersion: cluster.x-k8s.io/vlbetal
kind: Cluster 
metadata:
  name: kubernetes-cluster-qy5m
  namespace: namespace- 1
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        - 192.168.156.0/20  # what to use here
    services:
      cidrBlocks:
        - 10.96.0.0/12    # what to use here
    serviceDomain: cluster.local
  topology:
    class: builtin-generic-v3.3.0
    version: v1.32.0---vmware.6-fips-vkr.2 
    variables:
      - name: kubernetes
        value:
          certificateRotation:
            enabled: true
            renewalDaysBeforeExpiry: 90
      - name: vmClass
        value: best-effort-xsmall
      - name: storageClass
        value: k8s-storage-policy
    controlPlane: 
      replicas: 1 
      metadata:
        annotations:
          run.tanzu.vmware.com/resolve-os-image: os-name=photon, content-library=cl-5e990aa753a07bc9e.    # what to use here, necessary?
    workers:
      machineDeployments:
        - class: node-pool
          name: kubernetes-cluster-qy5m-nodepool-j0a7
          replicas: 2 
          metadata:
          annotations:
            run. tanzu.vmware.com/resolve-os-image: os-name=photon, content-library=cl-5e990aa753a07bc9e.   # what to use here, necessary?